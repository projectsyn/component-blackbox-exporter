parameters:
  blackbox_exporter:
    =_metadata:
      multi_instance: true

    namespace: syn-blackbox-exporter
    createNamespace: true

    charts:
      blackbox_exporter: 9.0.0

    helmValues:
      pspEnabled: false
      securityContext:
        runAsUser: null
        runAsGroup: null
