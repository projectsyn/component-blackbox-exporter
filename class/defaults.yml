parameters:
  blackbox_exporter:
    =_metadata:
      multi_instance: true
    charts:
      blackbox_exporter: 7.0.1
    helmValues: {}
    namespace: syn-blackbox-exporter
    createNamespace: true
