parameters:
  blackbox_exporter:
    =_metadata:
      multi_instance: true

    namespace: syn-blackbox-exporter
    createNamespace: true

    charts:
      blackbox_exporter: 7.2.0

    helmValues:
      pspEnabled: false
      securityContext:
        runAsUser: null
        runAsGroup: null
