parameters:
  blackbox_exporter:
    =_metadata:
      multi_instance: true
      multi_tenant: true

    namespace: syn-blackbox-exporter
    createNamespace: true

    charts:
      blackbox_exporter: 11.4.2

    helmValues:
      pspEnabled: false
      securityContext:
        runAsUser: null
        runAsGroup: null
