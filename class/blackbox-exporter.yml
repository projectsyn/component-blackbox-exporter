parameters:
  kapitan:
    compile:
      - input_paths:
          - blackbox-exporter/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - blackbox-exporter/component/main.jsonnet
        input_type: jsonnet
        output_path: blackbox-exporter/
